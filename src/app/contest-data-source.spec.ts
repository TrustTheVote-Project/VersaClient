import { ConstestDataSource } from './contest-data-source';

describe('ConstestDataSource', () => {
  it('should create an instance', () => {
    expect(new ConstestDataSource()).toBeTruthy();
  });
});
