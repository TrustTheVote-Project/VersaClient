export interface Candidate {
    id: string,
    name: string, 
    slogan?: string,
    conatact_info?: any,
    is_incumbent?: boolean,
    is_top_ticket?: boolean,
    party?: string,
    registration_status?: string
}
