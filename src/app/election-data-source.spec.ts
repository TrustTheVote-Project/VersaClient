import { ElectionDataSource } from './election-data-source';

describe('ElectionDataSource', () => {
  it('should create an instance', () => {
    expect(new ElectionDataSource()).toBeTruthy();
  });
});
